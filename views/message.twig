<html>
    <head>
        <script src="/socket.io/socket.io.js"></script>
    </head>
    <body>

        <label for="">Message:</label>
        <input type="text" id="my" onkeyup="typing()">
        <input type="submit" onclick="send()">
        
    </body>
    <script>
        const socket=io.connect()
name=prompt("votre nom :")
        socket.on("msg1",(data)=>{
            p=document.createElement("p")
            p.innerText=data
            document.body.append(p)
        })

          socket.on("typing",(data)=>{
            p=document.createElement("p")
            p.innerText=data
            document.body.append(p)
        })

         socket.on("m1",(data)=>{
            console.log(data)
            p=document.createElement("p")
            p.innerText=(data.name+":"+data.msg)
            document.body.append(p)
        })

        c=document.getElementById("my")

        function send(){
        socket.emit('m1',{name:name,msg:c.value})
        }
        function typing(){
            socket.emit('typing',"is typing ...")
        }

    </script>
</html>
